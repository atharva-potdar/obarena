{{- if .Values.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: sandbox-orchestrator
  namespace: platform
  labels:
    {{- include "iicpc.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: sandbox-orchestrator
  minReplicas: {{ .Values.hpa.sandboxOrchestrator.minReplicas }}
  maxReplicas: {{ .Values.hpa.sandboxOrchestrator.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.hpa.sandboxOrchestrator.cpuTarget }}
{{- end }}
