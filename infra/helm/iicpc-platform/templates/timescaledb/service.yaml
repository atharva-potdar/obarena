apiVersion: v1
kind: Service
metadata:
  name: timescaledb
  namespace: platform
  labels:
    {{- include "iicpc.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: timescaledb
  ports:
    - name: postgres
      port: 5432
