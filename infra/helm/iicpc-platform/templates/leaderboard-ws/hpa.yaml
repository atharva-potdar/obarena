{{- if .Values.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: leaderboard-ws
  namespace: platform
  labels:
    {{- include "iicpc.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: leaderboard-ws
  minReplicas: {{ .Values.hpa.leaderboardWs.minReplicas }}
  maxReplicas: {{ .Values.hpa.leaderboardWs.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.hpa.leaderboardWs.cpuTarget }}
{{- end }}
