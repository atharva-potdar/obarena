apiVersion: v1
kind: Service
metadata:
  name: leaderboard-ws
  namespace: platform
  labels:
    {{- include "obarena.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: leaderboard-ws
  ports:
    - name: http
      port: 8090
      targetPort: http
